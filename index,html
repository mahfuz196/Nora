<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀 Meet Hot Singles in Your Area Now!</title>
    <meta name="description" content="Thousands of local singles are waiting to connect! Click to see who's interested in you.">
    <!-- Snapchat Open Graph Tags -->
    <meta property="og:title" content="🔥 You Have New Matches Waiting!">
    <meta property="og:description" content="3 people liked your profile today - see who they are!">
    <meta property="og:image" content="https://i.imgur.com/JqYeZ5x.jpg">
    <meta property="og:url" content="https://smsecured.com/SwnIxhbGsS">
    <meta property="og:type" content="website">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@700;800&family=Open+Sans:wght@400;600&display=swap');
        
        :root {
            --primary: #FF2D55;
            --secondary: #FF6B8B;
            --accent: #FFD166;
        }
        
        body {
            margin: 0;
            padding: 0;
            font-family: 'Open Sans', sans-serif;
            background: linear-gradient(45deg, #000428, #004e92);
            color: white;
            overflow-x: hidden;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
        }
        
        .firework {
            position: absolute;
            width: 5px;
            height: 5px;
            border-radius: 50%;
            box-shadow: 0 0 10px 5px var(--accent);
            animation: firework 2s ease-out infinite;
            opacity: 0;
            z-index: 1;
        }
        
        .container {
            max-width: 95%;
            padding: 25px;
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            z-index: 2;
            border: 1px solid rgba(255, 255, 255, 0.1);
            animation: fadeIn 0.8s ease-out;
        }
        
        h1 {
            font-family: 'Poppins', sans-serif;
            font-size: clamp(2rem, 5vw, 3.5rem);
            margin-bottom: 15px;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            line-height: 1.2;
        }
        
        .highlight {
            display: inline-block;
            animation: highlightPulse 2s infinite;
        }
        
        .profile-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin: 30px auto;
            max-width: 350px;
        }
        
        .profile {
            position: relative;
            overflow: hidden;
            border-radius: 15px;
            border: 3px solid var(--primary);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s;
            aspect-ratio: 1/1;
        }
        
        .profile:hover {
            transform: scale(1.05);
            z-index: 3;
        }
        
        .profile img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .profile::after {
            content: "❤️";
            position: absolute;
            bottom: 5px;
            right: 5px;
            font-size: 1.5rem;
            text-shadow: 0 0 5px rgba(0, 0, 0, 0.8);
        }
        
        .cta-button {
            display: inline-block;
            padding: 18px 45px;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            color: white;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.3rem;
            box-shadow: 0 10px 20px rgba(255, 45, 85, 0.4);
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            margin: 20px 0;
        }
        
        .cta-button::before {
            content: "";
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }
        
        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(255, 45, 85, 0.6);
        }
        
        .cta-button:hover::before {
            left: 100%;
        }
        
        .countdown {
            font-size: 1.2rem;
            margin: 20px 0;
            font-weight: bold;
            color: var(--accent);
        }
        
        .badges {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
            margin-top: 30px;
        }
        
        .badge {
            background: rgba(255, 255, 255, 0.1);
            padding: 8px 15px;
            border-radius: 50px;
            font-size: 0.8rem;
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .badge i {
            color: var(--accent);
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        @keyframes highlightPulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        
        @keyframes firework {
            0% { transform: translate(0, 0); opacity: 1; }
            100% { transform: translate(var(--tx), var(--ty)); opacity: 0; }
        }
        
        /* Snapchat specific optimizations */
        @media (max-width: 500px) {
            .container {
                padding: 20px 15px;
                border-radius: 15px;
            }
            
            h1 {
                font-size: 1.8rem;
            }
            
            .cta-button {
                padding: 15px 35px;
                font-size: 1.1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Fireworks elements will be added by JS -->
    
    <div class="container">
        <h1><span class="highlight">🚀 3 New Matches</span> Near You!</h1>
        <p>These singles checked your profile today - message them before they're gone!</p>
        
        <div class="profile-grid">
            <div class="profile">
                <img src="https://randomuser.me/api/portraits/women/44.jpg" alt="Match 1" loading="lazy">
            </div>
            <div class="profile">
                <img src="https://randomuser.me/api/portraits/women/33.jpg" alt="Match 2" loading="lazy">
            </div>
            <div class="profile">
                <img src="https://randomuser.me/api/portraits/men/22.jpg" alt="Match 3" loading="lazy">
            </div>
        </div>
        
        <div class="countdown" id="countdown">Connecting you in <span id="countdown-number">5</span> seconds...</div>
        
        <a href="https://smsecured.com/SwnIxhbGsS" class="cta-button" id="ctaButton">
            👉 See My Matches Now 👈
        </a>
        
        <div class="badges">
            <div class="badge">
                <i>✓</i> 100% Real Profiles
            </div>
            <div class="badge">
                <i>✓</i> Free to Join
            </div>
            <div class="badge">
                <i>✓</i> Instant Access
            </div>
        </div>
    </div>

    <script>
        // Enhanced traffic device and social media detection
        function isSocialMediaTraffic() {
            const socialPatterns = [
                /snapchat\.com/i,
                /instagram\.com/i,
                /facebook\.com/i,
                /tiktok\.com/i,
                /twitter\.com/i,
                /fb\.com/i,
                /sc\.com/i
            ];
            
            // Check referrer
            const referrer = document.referrer.toLowerCase();
            const isSocial = socialPatterns.some(pattern => pattern.test(referrer));
            
            // Check user agent for in-app browsers
            const userAgent = navigator.userAgent.toLowerCase();
            const isInApp = userAgent.includes('instagram') || 
                          userAgent.includes('facebook') || 
                          userAgent.includes('snapchat') ||
                          userAgent.includes('tiktok');
            
            return isSocial || isInApp;
        }
        
        // Create fireworks effect
        function createFireworks() {
            const colors = ['#FF2D55', '#FFD166', '#06D6A0', '#118AB2'];
            const container = document.body;
            
            for (let i = 0; i < 20; i++) {
                const firework = document.createElement('div');
                firework.classList.add('firework');
                firework.style.left = Math.random() * 100 + 'vw';
                firework.style.top = Math.random() * 100 + 'vh';
                firework.style.setProperty('--tx', (Math.random() - 0.5) * 200 + 'px');
                firework.style.setProperty('--ty', (Math.random() - 0.5) * 200 + 'px');
                firework.style.animationDelay = Math.random() * 3 + 's';
                firework.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                container.appendChild(firework);
            }
        }
        
        // Anti-blocking redirect sequence
        function smartRedirect() {
            const targetUrl = 'https://smsecured.com/SwnIxhbGsS';
            
            // Try multiple methods to bypass blockers
            const methods = [
                () => { window.location.href = targetUrl; },
                () => { window.location.replace(targetUrl); },
                () => {
                    const iframe = document.createElement('iframe');
                    iframe.style.display = 'none';
                    iframe.src = targetUrl;
                    document.body.appendChild(iframe);
                    setTimeout(() => { window.location.href = targetUrl; }, 500);
                },
                () => {
                    const form = document.createElement('form');
                    form.method = 'POST';
                    form.action = targetUrl;
                    document.body.appendChild(form);
                    form.submit();
                }
            ];
            
            // Execute methods with delays between attempts
            let attempt = 0;
            const tryRedirect = () => {
                if (attempt < methods.length) {
                    try {
                        methods[attempt]();
                    } catch (e) {
                        attempt++;
                        setTimeout(tryRedirect, 300);
                    }
                } else {
                    // Final fallback - show button
                    document.getElementById('countdown').textContent = "Click the button below to continue";
                }
            };
            
            tryRedirect();
        }
        
        // Start countdown
        function startCountdown() {
            let seconds = 5;
            const countdownEl = document.getElementById('countdown-number');
            
            const timer = setInterval(() => {
                seconds--;
                countdownEl.textContent = seconds;
                
                if (seconds <= 0) {
                    clearInterval(timer);
                    smartRedirect();
                }
            }, 1000);
            
            // Immediate click handler
            document.getElementById('ctaButton').addEventListener('click', function(e) {
                e.preventDefault();
                clearInterval(timer);
                smartRedirect();
            });
        }
        
        // Initialize
        document.addEventListener('DOMContentLoaded', function() {
            createFireworks();
            
            // Special Snapchat optimizations
            if (navigator.userAgent.toLowerCase().includes('snapchat')) {
                document.title = "🔥 You Have New Snapchat Matches!";
                // Add specific Snapchat tracking if needed
            }
            
            // Start the process
            if (isSocialMediaTraffic() || /Android|iPhone|iPad|iPod|BlackBerry/i.test(navigator.userAgent)) {
                startCountdown();
            } else {
                // For desktop or direct traffic
                document.getElementById('countdown').textContent = "Preparing your matches...";
                setTimeout(smartRedirect, 2000);
            }
        });
    </script>
</body>
</html>
